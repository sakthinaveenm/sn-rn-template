import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 80,
  },
  input: {
    borderWidth: 1,
    borderColor: '#000000',
  },
});

export default styles;
